export interface ImageInfo {
  path: string;
  thumbnail: string;
  width: number;
  height: number;
  size: number;
}

export interface ImageMetadata {
  width: number;
  height: number;
  size: number;
}